{
  "name": "2_sequelize_express",
  "version": "1.0.0",
  "description": "Companion to 0_pg_express - same Express app and helpers interface, but the database layer is rewritten using the Sequelize ORM instead of raw pg queries.",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server_express.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch server_express.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "pg": "^8.21.0",
    "sequelize": "^6.37.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
