services:
  server:
    build: .
    ports:
      - '8080:8080'
    depends_on:
      - db
  db:
    image: 'postgres:17'
    ports: 
      - '5432:5432'
    environment:
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: 'root'